<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ProductImageDao">
    <select id="getFirstImage" resultType="com.pojo.ProductImage" >
        select * from productimage where pid = #{id} and type=#{type}
    </select>
</mapper>
